{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.orbitchain.org/events/refund_processed.json",
  "title": "OrbitChain Event: refund_processed",
  "description": "Emitted when a refund is processed for a donor.",
  "type": "object",
  "required": [
    "donor",
    "amount",
    "asset",
    "ledger"
  ],
  "properties": {
    "donor": {
      "type": "string",
      "description": "Donor's Stellar address (G...)"
    },
    "amount": {
      "type": "string",
      "description": "Refund amount in base units, encoded as a decimal string",
      "format": "int128"
    },
    "asset": {
      "type": "string",
      "description": "Asset selector: \"Native\" for XLM, or a Stellar token contract address"
    },
    "ledger": {
      "type": "integer",
      "description": "Ledger number at which the refund was processed",
      "format": "uint32",
      "minimum": 0
    }
  },
  "additionalProperties": false
}
